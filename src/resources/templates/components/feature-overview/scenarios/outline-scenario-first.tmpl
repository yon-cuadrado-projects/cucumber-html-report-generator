<tr>
    <th class="info-color white-text"></th>
    <% var cells = scenario.examples[0]; %>
    <% for( var k = 0; k < cells.length; k++ ) { %>
    <th class="info-color white-text"><%= cells[k] %></th>
    <% } %>
    <th></th>

</tr>

<% var rows = scenario.examples; %>
<% for( var j = 1; j < rows.length; j++ ) { %>
<% scenarioIndex = scenarioIndex + 1; %>
<tr data-bs-toggle="collapse" data-bs-target="#demo1-<%= scenarioIndex %>-<%= scenarioIndex %>"
    class="accordion-toggle" aria-expanded="true">
    <td><button class="btn btn-default btn-xs"><span class="glyphicon glyphicon-eye-open"></span></button>
    </td>
    <% var cells = rows[j]; %>
    <% for( var k = 0; k < cells.length; k++ ) { %>
    <td><%= cells[k] %></td>
    <% } %>
    <td>
        <%= scenarioTemplates.get(scenarioIndex).get("Results") %>
    </td>
</tr>